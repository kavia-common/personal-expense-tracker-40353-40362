name: frontend_flutter
description: "SmartSpender - Personal expense tracker (sample data)."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  # Existing deps retained (not heavily used but kept for template compatibility)
  sqflite: ^2.3.2
  path: ^1.9.0
  flutter_dotenv: ^5.1.0
  flutter_staggered_grid_view: ^0.7.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/
